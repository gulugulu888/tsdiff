重大进展！训练和最终评估都成功完成了！

这是非常棒的结果，意味着我们已经打通了从数据预处理到模型训练再到最终评估的整个流程。

日志分析和解读：

训练完成：

Epoch 29/29 ...

2025-05-23 17:03:13 [INFO] __main__: Training completed successfully!

模型按预期完成了所有训练周期。

检查点保存成功：

[Backup] Saved epoch X to ...：您的 ForceCheckpointCallback 成功保存了每个 epoch 的备份。

2025-05-23 17:03:13 [INFO] __main__: Found best checkpoint from callback: D:\FDR_TS_PREDICT\unconditional-time-series-diffusion\results\CAS_250L_run01\train_CAS_250L\checkpoints\epoch=epoch=029-loss=train_loss_epoch=0.6460.ckpt

这是关键！ModelCheckpoint 现在能够根据 train_loss_epoch 正确地识别并记录“最佳”模型（在这个短时训练中，它可能是最后一个 epoch 的模型，因为 train_loss_epoch 可能没有足够的时间来展示明显的“最佳点”）。

Manually saved final checkpoint ... 和 Saved model weights ... 也都成功了。

最终评估 (run_final_evaluation) 成功执行：

2025-05-23 17:03:13 [INFO] __main__: Running final evaluation with 100 samples.

100%|██████████████████████████████████████████████████████████████████████████████████████████████████████████| 116/116 [05:10<00:00, 2.67s/it]

这表明在测试集（116个序列）上生成100个样本的预测花费了大约5分10秒。这对于CPU上的KeOps来说是正常的，因为采样过程涉及到多次模型前向传播。

2025-05-23 17:08:24 [INFO] __main__: Final Evaluation Metrics: {'MSE': 1.71198987646283e-22, 'mean_wQuantileLoss': inf, 'ND': inf, 'NRMSE': inf}

MSE 非常接近于0： 这通常意味着模型的预测值与真实值非常接近。

归一化指标 (ND, NRMSE, CRPS/mean_wQuantileLoss) 为 inf： 这与我们在 EvaluateCallback 中观察到的现象一致，强烈暗示测试集中的许多序列（或其预测窗口部分）的真实目标值 (target) 非常接近于零，导致这些指标的分母为零。

日志和摘要保存成功：

Training run summary saved to ...

All results, logs, and checkpoints saved in ...

总结与下一步：

您已经成功地完成了 TSDiff 模型的单变量训练和评估流程！这是一个非常重要的里程碑。

现在的主要任务是理解模型的预测结果和评估指标的含义，并为后续的改进做准备。

深入分析评估指标为 inf 的原因：

运行我之前提供的脚本来检查测试集 (test.jsonl) 中 CAS 目标窗口内零值的比例。

import json
import numpy as np
from pathlib import Path

# --- 配置 ---
processed_data_dir = Path("./data/processed_fdr_interactive/CAS_origfreq") # 您的数据路径
test_file = processed_data_dir / "test.jsonl"
prediction_length = 40 # 从您的配置中获取
# --- 结束配置 ---

zero_target_in_prediction_window_count = 0
near_zero_target_in_prediction_window_count = 0
non_zero_sum_count = 0
total_test_series = 0
problematic_series_ids_zero = []
problematic_series_ids_near_zero = []
abs_target_sums = []

if not test_file.exists():
    print(f"Test file not found: {test_file}")
    return

with open(test_file, 'r', encoding='utf-8') as f:
    for line_idx, line in enumerate(f):
        total_test_series += 1
        entry = json.loads(line)
        target = np.array(entry["target"], dtype=np.float32)
        
        item_id = entry.get("item_id", f"series_{line_idx}")

        if len(target) >= prediction_length:
            prediction_window_target = target[-prediction_length:]
            
            abs_target_sum_window = np.sum(np.abs(prediction_window_target))
            abs_target_sums.append(abs_target_sum_window)

            if abs_target_sum_window == 0.0: # 完全为零
                zero_target_in_prediction_window_count += 1
                problematic_series_ids_zero.append(item_id)
            elif abs_target_sum_window < 1e-5: # 非常接近零 (阈值可调)
                near_zero_target_in_prediction_window_count += 1
                problematic_series_ids_near_zero.append(item_id)
            else:
                non_zero_sum_count +=1
        elif len(target) > 0 : 
            abs_target_sum_window = np.sum(np.abs(target))
            abs_target_sums.append(abs_target_sum_window)
            if abs_target_sum_window == 0.0:
                 zero_target_in_prediction_window_count += 1
                 problematic_series_ids_zero.append(item_id)
            elif abs_target_sum_window < 1e-5:
                 near_zero_target_in_prediction_window_count += 1
                 problematic_series_ids_near_zero.append(item_id)
            else:
                non_zero_sum_count +=1
        else: # target 数组为空
            abs_target_sums.append(0.0)
            zero_target_in_prediction_window_count += 1
            problematic_series_ids_zero.append(item_id)


print(f"\n--- Target Value Analysis for Test Set ({test_file}) ---")
print(f"Total test series checked: {total_test_series}")
print(f"Series with EXACTLY zero target sum in prediction window: {zero_target_in_prediction_window_count}")
print(f"Series with NEARLY zero target sum (<1e-5) in prediction window: {near_zero_target_in_prediction_window_count}")
print(f"Series with non-zero target sum in prediction window: {non_zero_sum_count}")

if total_test_series > 0:
    perc_zero = zero_target_in_prediction_window_count / total_test_series
    perc_near_zero = near_zero_target_in_prediction_window_count / total_test_series
    print(f"Percentage of series with zero-sum target in prediction window: {perc_zero:.2%}")
    print(f"Percentage of series with near-zero-sum target in prediction window: {perc_near_zero:.2%}")

if problematic_series_ids_zero:
    print(f"IDs of series with EXACTLY zero target sum (first 5): {problematic_series_ids_zero[:5]}")
if problematic_series_ids_near_zero:
    print(f"IDs of series with NEARLY zero target sum (first 5): {problematic_series_ids_near_zero[:5]}")

if abs_target_sums:
    plt.figure(figsize=(10,6))
    plt.hist(abs_target_sums, bins=50, log=True) # Use log scale for y-axis if sums vary a lot
    plt.title("Distribution of Absolute Target Sums in Prediction Window (Test Set)")
    plt.xlabel("Absolute Sum of Target in Prediction Window")
    plt.ylabel("Frequency (Log Scale)")
    plt.grid(True, alpha=0.5)
    plot_path = processed_data_dir / "test_set_target_sum_distribution.png"
    plt.savefig(plot_path)
    print(f"Saved distribution plot to: {plot_path}")
    plt.close()


将此脚本保存为例如 custom_scripts/check_zero_targets.py，然后运行它。

如果大量序列的目标窗口确实是零，那么 ND, NRMSE, CRPS 为 inf 是正常的。在这种情况下，MSE 是一个更值得关注的指标。MSE: 1.71e-22 是一个极小的值，表明对于那些目标非零的序列，或者当目标和预测都为零时，模型的误差非常小。

使用 custom_scripts/run_visualization.py 进行可视化：

修改 run_visualization.py：

确保 config_path 指向 configs/fdr/train_CAS_250L.yaml。

确保 ckpt_path 指向您训练好的检查点，例如：
results/CAS_250L_run01/lightning_logs/version_18/train_CAS_250L/checkpoints/epoch=epoch=029-loss=train_loss_epoch=0.6460.ckpt
或者您手动保存的 final_manual.ckpt 或 model_weights.pth (如果是 model_weights.pth，加载方式可能需要调整，因为它只包含 state_dict)。优先使用 .ckpt 文件，因为它包含完整的模型和超参数。

确保 test_path 指向 data/processed_fdr_interactive/CAS_origfreq/test.jsonl。

运行可视化脚本：

python custom_scripts/run_visualization.py
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Bash
IGNORE_WHEN_COPYING_END

观察预测：

对于那些目标值为零的序列，模型的预测是否也接近于零？

对于那些目标值非零的序列，模型的预测与真实值的吻合程度如何？

可视化可以帮助您直观地理解为什么某些指标是 inf。

（如果需要）调整评估指标的显示：

如果归一化指标持续为 inf，您可以修改 visualization.py 中的 evaluate_model 函数（或者它调用的任何评估逻辑），使其更侧重于显示和报告 MSE 和 MAE。

（可选）进一步训练或调整：

训练更长时间： 30个 epoch 可能不足以让模型完全收敛。您可以增加 max_epochs 到例如 100 或 200，然后重新训练（记得使用新的 --out_dir 或删除旧的 version_X 目录以避免混淆）。

学习率： 观察 TensorBoard 中的损失曲线。如果损失在早期就太平坦，可以尝试稍大的初始学习率（例如 5e-4）。如果损失波动很大，可以尝试更小的学习率。

模型容量： 如果 diffusion_small_config 的结果不理想，并且您有足够的计算资源，可以尝试在配置文件中将 diffusion_config 改为 diffusion_s4_config（这会使用更大的S4骨干网络）。

总结：

您已经成功地让模型跑通了整个训练流程！现在的主要任务是：

确认并理解为什么归一化评估指标是 inf（通过检查测试集中的零值目标）。

通过可视化来直观评估模型的预测质量。

根据这些分析结果，决定是否需要进一步调整模型配置、训练参数或数据处理策略。

您做得非常好！解决这些复杂模型的初始设置问题本身就是一项挑战。